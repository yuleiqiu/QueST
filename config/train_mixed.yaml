defaults:
  - train_base
  - _self_

stage: 1

logging_folder: null

training:
  n_epochs: 100

rollout:
  interval: 20 # rollout every ${rollout.interval} epochs
  rollouts_per_env: 5
  num_parallel_envs: 5

logging:
  project: mixed_datasets